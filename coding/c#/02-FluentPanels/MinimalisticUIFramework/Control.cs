namespace MinimalisticUIFramework {
	public abstract class Control {
		// Note: if necessary, you can add your code here - but keep Control as simple as possible!
		// IMPORTANT NOTE: This Control class itself should NOT expect/depend on existance of any specific classes inheriting from Control.
	}
}